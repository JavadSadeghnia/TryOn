# 🔄 Virtual Try-On App - Complete Workflow

## Visual Setup Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    INITIAL SETUP (One Time)                  │
└─────────────────────────────────────────────────────────────┘

Step 1: Install Android Studio
    ↓
[Download] → [Install] → [Setup Wizard] → [SDK Download]
    ↓
✅ Android Studio Ready


Step 2: Open Project
    ↓
[Open Android Studio] → [Import Project] → [Gradle Sync]
    ↓
✅ Project Ready to Build
```

---

## Development Workflow

### Every Time You Want to Use the App

```
┌──────────────────────┐
│  Start Colab Backend │
└──────────────────────┘
         ↓
    Open Colab
         ↓
    Upload notebook
         ↓
    Enable GPU
         ↓
    Run all cells
         ↓
    Copy URL: https://xxxxx.gradio.live
         ↓
┌──────────────────────┐
│   Keep Tab Open!     │ ← Important!
└──────────────────────┘


┌──────────────────────┐
│   Build & Run App    │
└──────────────────────┘
         ↓
    Open Android Studio
         ↓
    Select device
         ↓
    Click Run ▶️
         ↓
    Wait for build
         ↓
    App launches on device
         ↓
┌──────────────────────┐
│   Configure & Test   │
└──────────────────────┘
         ↓
    Tap Settings ⚙️
         ↓
    Paste Gradio URL
         ↓
    Upload body photo
         ↓
    Upload clothing
         ↓
    Tap "Generate"
         ↓
    View result! 🎉
```

---

## App Usage Flow (User Perspective)

```
┌─────────────────────────────────────────────────────────┐
│                    Open App                              │
└─────────────────────────────────────────────────────────┘
                         ↓

┌─────────────────────────────────────────────────────────┐
│              First Time Setup                            │
│  • Tap Settings icon (⚙️)                               │
│  • Enter Gradio API URL                                  │
│  • Save                                                  │
│  ✅ See "API Connected" message                          │
└─────────────────────────────────────────────────────────┘
                         ↓

┌─────────────────────────────────────────────────────────┐
│              Upload Body Photo                           │
│  • Tap "Body Photo" card                                │
│  • Choose: Camera or Gallery                             │
│  • Grant permissions if asked                            │
│  • Select/Take photo                                     │
│  ✅ See photo preview in card                            │
└─────────────────────────────────────────────────────────┘
                         ↓

┌─────────────────────────────────────────────────────────┐
│            Upload Clothing Image                         │
│  • Tap "Clothing Item" card                             │
│  • Choose: Camera or Gallery                             │
│  • Select/Take photo                                     │
│  ✅ See photo preview in card                            │
└─────────────────────────────────────────────────────────┘
                         ↓

┌─────────────────────────────────────────────────────────┐
│            Generate Try-On                               │
│  • Both images selected                                  │
│  • Button turns blue (enabled)                           │
│  • Tap "Generate Try-On"                                │
│  ⏳ Loading... (10-30 seconds)                           │
└─────────────────────────────────────────────────────────┘
                         ↓

┌─────────────────────────────────────────────────────────┐
│              View Result                                 │
│  🎉 Generated image appears                              │
│  • View the try-on result                               │
│  • Try with different photos                             │
└─────────────────────────────────────────────────────────┘
```

---

## Technical Data Flow

```
┌──────────────┐
│  User Device │
│   (Android)  │
└──────┬───────┘
       │
       │ 1. User uploads
       │    two images
       ↓
┌──────────────────┐
│   HomeScreen.kt  │
│   (UI Layer)     │
└──────┬───────────┘
       │
       │ 2. Calls
       │    generateTryOn()
       ↓
┌──────────────────────┐
│  TryOnViewModel.kt   │
│  (Business Logic)    │
└──────┬───────────────┘
       │
       │ 3. Converts URIs
       │    to Files
       ↓
┌──────────────────┐
│  FileUtils.kt    │
│  (Utilities)     │
└──────┬───────────┘
       │
       │ 4. Creates
       │    multipart request
       ↓
┌──────────────────────┐
│  RetrofitClient.kt   │
│  (Network Layer)     │
└──────┬───────────────┘
       │
       │ 5. HTTP POST
       │    with images
       ↓
┌──────────────────────────────┐
│   Google Colab               │
│   • Gradio receives request  │
│   • Processes with AI        │
│   • Returns JPEG image       │
└──────┬───────────────────────┘
       │
       │ 6. Returns
       │    image bytes
       ↓
┌──────────────────────┐
│  TryOnViewModel.kt   │
│  (Updates state)     │
└──────┬───────────────┘
       │
       │ 7. StateFlow
       │    emits Success
       ↓
┌──────────────────┐
│  HomeScreen.kt   │
│  (Displays)      │
└──────┬───────────┘
       │
       │ 8. Shows result
       │    to user
       ↓
┌──────────────┐
│  User sees   │
│  try-on! 🎉  │
└──────────────┘
```

---

## Build Process Flow

```
┌────────────────────────────────────────────────────┐
│  Click Run ▶️ in Android Studio                    │
└────────────────────────────────────────────────────┘
                    ↓
        ┌───────────────────────┐
        │  Gradle Build Process │
        └───────────────────────┘
                    ↓
    ┌───────────────────────────────┐
    │  1. Read build.gradle.kts     │
    │     • Check dependencies      │
    │     • Download if needed      │
    └───────────────────────────────┘
                    ↓
    ┌───────────────────────────────┐
    │  2. Compile Kotlin Code       │
    │     • .kt → .class files      │
    │     • Check for errors        │
    └───────────────────────────────┘
                    ↓
    ┌───────────────────────────────┐
    │  3. Process Resources         │
    │     • XML files               │
    │     • Strings, themes, etc.   │
    └───────────────────────────────┘
                    ↓
    ┌───────────────────────────────┐
    │  4. Generate R.java           │
    │     • Resource IDs            │
    └───────────────────────────────┘
                    ↓
    ┌───────────────────────────────┐
    │  5. Package APK               │
    │     • Combine everything      │
    │     • Sign with debug key     │
    └───────────────────────────────┘
                    ↓
    ┌───────────────────────────────┐
    │  6. Install on Device         │
    │     • Via ADB                 │
    └───────────────────────────────┘
                    ↓
    ┌───────────────────────────────┐
    │  7. Launch App                │
    │     • MainActivity starts     │
    └───────────────────────────────┘
                    ↓
        ✅ App Running!
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────┐
│         TryOnViewModel State                     │
└─────────────────────────────────────────────────┘

                ┌──────────────┐
                │  Initial     │
                │  State       │
                └──────┬───────┘
                       │
        User selects body image
                       ↓
                ┌──────────────┐
                │ Body Image   │
                │ URI set      │
                └──────┬───────┘
                       │
        User selects clothing
                       ↓
                ┌──────────────┐
                │ Clothing URI │
                │ set          │
                └──────┬───────┘
                       │
        User taps Generate
                       ↓
                ┌──────────────┐
                │  Loading     │
                │  State       │
                └──────┬───────┘
                       │
                API processes
                       │
        ┌──────────────┴──────────────┐
        │                             │
    Success                        Error
        │                             │
        ↓                             ↓
┌──────────────┐            ┌──────────────┐
│   Success    │            │    Error     │
│   State      │            │    State     │
│ (Show image) │            │ (Show msg)   │
└──────────────┘            └──────────────┘
```

---

## Permission Flow

```
┌─────────────────────────────────────────────────┐
│         First Time App Launch                    │
└─────────────────────────────────────────────────┘
                    ↓
        User taps "Take Photo"
                    ↓
    ┌───────────────────────────────┐
    │  Check Camera Permission      │
    └───────────┬───────────────────┘
                │
        ┌───────┴────────┐
        │                │
    Granted         Not Granted
        │                │
        │                ↓
        │    ┌──────────────────────┐
        │    │  Request Permission  │
        │    └──────────┬───────────┘
        │               │
        │       ┌───────┴────────┐
        │       │                │
        │   Granted          Denied
        │       │                │
        │       │                ↓
        │       │    ┌──────────────────┐
        │       │    │  Show Toast:     │
        │       │    │  "Permission     │
        │       │    │   Required"      │
        │       │    └──────────────────┘
        │       │
        └───────┘
                ↓
    ┌───────────────────────────────┐
    │  Launch Camera                │
    └───────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────┐
│         API Call (generateTryOn)                 │
└─────────────────────────────────────────────────┘
                    ↓
            Try Block {
                    ↓
        Validate Inputs
                    │
        ┌───────────┴──────────┐
        │                      │
    Valid               Invalid
        │                      │
        ↓                      ↓
    Continue          Error: "Select both images"
        │
        ↓
    Convert URIs to Files
        │
        ↓
    Create Multipart Request
        │
        ↓
    Make API Call
        │
    ┌───┴────┐
    │        │
Success    Failure
    │        │
    │        ↓
    │   ┌─────────────────┐
    │   │ HTTP Error?     │
    │   │ Network Error?  │
    │   │ Timeout?        │
    │   └────────┬────────┘
    │            │
    │            ↓
    │   Error: "Server error"
    │   Error: "Network error"
    │   Error: "Timeout"
    │
    ↓
} Catch (Exception e) {
    │
    ↓
Error: e.message
    │
    ↓
┌───────────────────┐
│  Update StateFlow │
│  with Error       │
└───────────────────┘
```

---

## Colab Session Lifecycle

```
┌─────────────────────────────────────┐
│  Open Colab & Upload Notebook       │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│  Select GPU Runtime                 │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│  Run Cells (Install Dependencies)   │
│  ⏱ Takes 2-3 minutes                │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│  Load AI Model                      │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│  Start Gradio Server                │
│  • Share = True                     │
│  • Creates public URL               │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│  Session Active                     │
│  ⏱ Lasts ~1 hour                    │
│  📍 URL: https://xxx.gradio.live    │
└─────────────────────────────────────┘
                │
                │ After 1 hour or if closed
                ↓
┌─────────────────────────────────────┐
│  Session Expires                    │
│  ❌ URL stops working                │
└─────────────────────────────────────┘
                ↓
        Need to restart?
                ↓
┌─────────────────────────────────────┐
│  Re-run Cells                       │
│  • Get new URL                      │
│  • Update in Android app            │
└─────────────────────────────────────┘
```

---

## Directory Structure Navigation

```
TryOn/
│
├── 📄 START_HERE.md ← Read this first!
├── 📄 QUICK_START.md
├── 📄 README.md
│
├── 📓 VirtualTryOn_Colab.ipynb ← Upload to Colab
│
├── 🔧 build.gradle.kts
├── 🔧 settings.gradle.kts
│
└── 📁 app/
    ├── 🔧 build.gradle.kts ← Dependencies
    │
    └── src/main/
        ├── AndroidManifest.xml ← Permissions
        │
        ├── 📁 java/com/tryon/virtualfit/
        │   ├── MainActivity.kt ← Entry point
        │   │
        │   ├── 📁 viewmodel/
        │   │   └── TryOnViewModel.kt ← Logic
        │   │
        │   ├── 📁 ui/screens/
        │   │   └── HomeScreen.kt ← UI
        │   │
        │   ├── 📁 network/
        │   │   ├── TryOnApiService.kt ← API
        │   │   └── RetrofitClient.kt
        │   │
        │   └── 📁 utils/
        │       └── FileUtils.kt
        │
        └── 📁 res/
            ├── values/strings.xml ← Text
            └── xml/file_paths.xml
```

---

## Quick Reference

### Commands

```bash
# Verify setup
./verify-setup.sh

# Build debug APK (if you have Gradle set up)
./gradlew assembleDebug

# Install on device
./gradlew installDebug
```

### URLs to Know

- Android Studio: https://developer.android.com/studio
- Google Colab: https://colab.research.google.com/
- Gradio Docs: https://gradio.app/docs/

### Important Files to Edit

| Want to change... | Edit this file... |
|------------------|-------------------|
| UI Colors | `app/src/main/java/com/tryon/virtualfit/ui/theme/Color.kt` |
| Text/Strings | `app/src/main/res/values/strings.xml` |
| Business Logic | `app/src/main/java/com/tryon/virtualfit/viewmodel/TryOnViewModel.kt` |
| UI Layout | `app/src/main/java/com/tryon/virtualfit/ui/screens/HomeScreen.kt` |

---

## Timeline

**Total Time: ~30 minutes**

| Task | Time |
|------|------|
| Install Android Studio | 15 min |
| Start Colab backend | 5 min |
| Open & sync project | 5 min |
| Build & run | 2 min |
| Test | 3 min |

---

## Success Indicators

✅ **You're successful when:**
- Android Studio opens without errors
- Gradle sync completes
- Colab shows a public URL
- App builds without errors
- App launches on device
- You can upload images
- Generated image appears

---

For detailed step-by-step instructions, see **[START_HERE.md](START_HERE.md)**!
